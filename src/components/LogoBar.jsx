import React from "react";

export default function LogoBar({fontSize}) {
  return <div className="text-center">
    <h1 className={`py-12 ${fontSize} tracking-[0.125em] md:tracking-[0.3em]`}>happy kids</h1>
  </div>;
}
